export const triggerWebhook = async (customer) => {
    // Mock webhook functionality (replace with real logic)
    console.log(`Webhook triggered for customer ${customer.name}`);
    // Example: Send POST request to admin with customer data
  };
  